# 사주 풀이 앱 기획서

## 1. 프로젝트 개요

### 1.1 프로젝트명
**신경을 (神經乙) - AI 기반 사주 풀이 서비스**

### 1.2 목적
전통적인 사주명리학과 현대 기술을 결합하여, 사용자에게 개인화된 운세 분석과 인생 가이드를 제공하는 웹 애플리케이션

### 1.3 핵심 가치
- **정확성**: 전통 사주명리학 알고리즘 기반
- **접근성**: 누구나 쉽게 이용할 수 있는 직관적인 UI/UX
- **개인화**: 개인별 맞춤 분석 및 추천

## 2. 기술 스택

### 2.1 프론트엔드
- **Framework**: Next.js 15+ (App Router)
- **Language**: TypeScript (Type-stripping 적용)
- **Styling**: Tailwind CSS
- **State Management**: React Context API / Zustand
- **UI Components**: Radix UI / shadcn/ui

### 2.2 BFF (Backend for Frontend)
- **Framework**: Koa.js
- **Language**: TypeScript (Type-stripping)
- **Validation**: Zod
- **API Documentation**: OpenAPI/Swagger

### 2.3 백엔드 서비스
- **사주 계산 엔진**: Node.js 기반 사주명리학 라이브러리
- **Database**: PostgreSQL (사용자 데이터)
- **Authentication**: JWT 기반 인증

### 2.4 인프라
- **Hosting**: Proxmox 기반 자체 호스팅
  - VM 또는 LXC 컨테이너
  - Docker Compose로 서비스 관리
- **Reverse Proxy**: Nginx 또는 Traefik
- **SSL**: Let's Encrypt (자동 갱신)
- **CI/CD**: GitHub Actions → Self-hosted runner
- **Monitoring**:
  - Sentry (에러 추적)
  - Prometheus + Grafana (시스템 모니터링)
  - Uptime Kuma (서비스 헬스 체크)

## 3. 주요 기능

### 3.1 사주 계산 (Core Feature)
**기능 설명**
- 생년월일시 입력 (양력/음력 선택)
- 사주팔자 자동 계산
- 천간지지, 오행 분석
- 십성, 십이운성 분석

**화면 구성**
- 날짜/시간 입력 폼
- 양력/음력 토글
- 성별 선택
- 출생지 정보 (시간 보정용)

### 3.2 운세 분석
**기능 설명**
- 오늘/이번주/이번달/올해 운세
- 연애운, 재물운, 직업운, 건강운
- 길일/흉일 표시
- 추천 행동 및 주의사항

**화면 구성**
- 운세 대시보드
- 카테고리별 탭
- 시각화 차트 (운세 그래프)

### 3.3 사주 풀이
**기능 설명**
- 상세한 사주 해석
- 성격 분석
- 재물운/직업운 장기 전망
- 궁합 좋은 띠/오행
- 개운 방법 (색상, 방위, 직업 등)

**화면 구성**
- 사주팔자 표
- 오행 차트 (오각형 그래프)
- 카테고리별 상세 해석
- PDF 다운로드 기능

### 3.4 궁합 보기
**기능 설명**
- 두 사람의 사주 비교
- 궁합 점수 계산
- 관계 조언
- 주의할 점

**화면 구성**
- 상대방 정보 입력
- 궁합 결과 표시
- 조언 및 해결책

### 3.5 오늘의 운세 (간편 조회)
**기능 설명**
- 빠른 오늘의 운세 조회
- 띠별 운세
- 별자리별 운세 (부가 기능)

### 3.6 마이페이지
**기능 설명**
- 저장된 사주 관리
- 즐겨찾기한 사주
- 이력 조회
- 설정 (알림, 테마)

## 4. 화면 설계

### 4.1 홈페이지
```
┌─────────────────────────────────────┐
│         신경을 (神經乙)              │
├─────────────────────────────────────┤
│   [오늘의 운세] [사주보기] [궁합]    │
├─────────────────────────────────────┤
│                                     │
│     생년월일을 입력해주세요          │
│                                     │
│   ┌──────────────────────┐          │
│   │ YYYY-MM-DD HH:MM     │          │
│   └──────────────────────┘          │
│                                     │
│   [양력/음력] [남/여]               │
│                                     │
│        [사주 보기]                  │
│                                     │
├─────────────────────────────────────┤
│  무료 사주풀이 | 운세 | 궁합        │
└─────────────────────────────────────┘
```

### 4.2 사주 결과 페이지
```
┌─────────────────────────────────────┐
│         내 사주팔자                  │
├─────────────────────────────────────┤
│  시주  일주  월주  년주             │
│  甲子  乙丑  丙寅  丁卯             │
├─────────────────────────────────────┤
│                                     │
│   [오행 밸런스 차트]                │
│        木 火 土 金 水               │
│                                     │
├─────────────────────────────────────┤
│   탭: [전체] [성격] [재물] [직업]   │
│                                     │
│   상세 풀이 내용...                 │
│                                     │
└─────────────────────────────────────┘
```

### 4.3 운세 대시보드
```
┌─────────────────────────────────────┐
│         오늘의 운세                  │
├─────────────────────────────────────┤
│  오늘  │  주간  │  월간  │  연간    │
├─────────────────────────────────────┤
│                                     │
│  종합운: ★★★★☆ (80점)             │
│                                     │
│  💝 연애운: ★★★☆☆               │
│  💰 재물운: ★★★★☆               │
│  💼 직업운: ★★★☆☆               │
│  🏥 건강운: ★★★★★               │
│                                     │
│  [오늘의 조언]                      │
│  새로운 만남이 기다리고 있습니다...  │
│                                     │
└─────────────────────────────────────┘
```

## 5. API 설계

### 5.1 BFF API 엔드포인트

#### 사주 계산
```
POST /api/saju/calculate
Request:
{
  "birthDate": "1990-01-01",
  "birthTime": "14:30",
  "isLunar": false,
  "gender": "male",
  "birthPlace": {
    "lat": 37.5665,
    "lng": 126.9780
  }
}

Response:
{
  "saju": {
    "year": { "heavenly": "庚", "earthly": "午" },
    "month": { "heavenly": "戊", "earthly": "寅" },
    "day": { "heavenly": "乙", "earthly": "未" },
    "hour": { "heavenly": "癸", "earthly": "未" }
  },
  "elements": {
    "wood": 2,
    "fire": 3,
    "earth": 2,
    "metal": 1,
    "water": 1
  },
  "analysis": {...}
}
```

#### 운세 조회
```
GET /api/fortune/today?userId={userId}
GET /api/fortune/weekly?userId={userId}
GET /api/fortune/monthly?userId={userId}
GET /api/fortune/yearly?userId={userId}

Response:
{
  "overall": 80,
  "love": 75,
  "wealth": 85,
  "career": 70,
  "health": 90,
  "luckyColors": ["red", "gold"],
  "luckyNumbers": [3, 7, 9],
  "advice": "...",
  "warning": "..."
}
```

#### 궁합 계산
```
POST /api/compatibility/calculate
Request:
{
  "person1": {
    "birthDate": "1990-01-01",
    "birthTime": "14:30",
    "gender": "male"
  },
  "person2": {
    "birthDate": "1992-05-15",
    "birthTime": "09:00",
    "gender": "female"
  }
}

Response:
{
  "score": 85,
  "compatibility": "매우 좋음",
  "strengths": [...],
  "warnings": [...],
  "advice": "..."
}
```

#### 사주 저장/조회
```
POST /api/users/saju
GET /api/users/saju/:id
PUT /api/users/saju/:id
DELETE /api/users/saju/:id
```

## 6. 데이터 모델

### 6.1 User
```typescript
interface User {
  id: string;
  email?: string;
  nickname?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

### 6.2 SajuRecord
```typescript
interface SajuRecord {
  id: string;
  userId: string;
  name: string;
  birthDate: Date;
  birthTime: string;
  isLunar: boolean;
  gender: 'male' | 'female';
  birthPlace?: {
    lat: number;
    lng: number;
  };
  saju: {
    year: Pillar;
    month: Pillar;
    day: Pillar;
    hour: Pillar;
  };
  elements: Elements;
  isFavorite: boolean;
  createdAt: Date;
  updatedAt: Date;
}

interface Pillar {
  heavenly: string; // 천간
  earthly: string;  // 지지
}

interface Elements {
  wood: number;  // 목
  fire: number;  // 화
  earth: number; // 토
  metal: number; // 금
  water: number; // 수
}
```

### 6.3 FortuneCache
```typescript
interface FortuneCache {
  id: string;
  sajuRecordId: string;
  date: Date;
  type: 'daily' | 'weekly' | 'monthly' | 'yearly';
  data: FortuneData;
  createdAt: Date;
}
```

## 7. 프로젝트 구조

```
saju-app/
├── apps/
│   ├── web/                    # Next.js 프론트엔드
│   │   ├── app/
│   │   │   ├── (main)/
│   │   │   │   ├── page.tsx           # 홈페이지
│   │   │   │   ├── saju/
│   │   │   │   │   ├── page.tsx       # 사주 입력
│   │   │   │   │   └── [id]/
│   │   │   │   │       └── page.tsx   # 사주 결과
│   │   │   │   ├── fortune/
│   │   │   │   │   └── page.tsx       # 운세
│   │   │   │   ├── compatibility/
│   │   │   │   │   └── page.tsx       # 궁합
│   │   │   │   └── my/
│   │   │   │       └── page.tsx       # 마이페이지
│   │   │   ├── api/                   # API routes (프록시)
│   │   │   └── layout.tsx
│   │   ├── components/
│   │   │   ├── ui/                    # shadcn/ui 컴포넌트
│   │   │   ├── saju/
│   │   │   │   ├── SajuCard.tsx
│   │   │   │   ├── ElementChart.tsx
│   │   │   │   └── PillarDisplay.tsx
│   │   │   ├── fortune/
│   │   │   │   ├── FortuneDashboard.tsx
│   │   │   │   └── FortuneChart.tsx
│   │   │   └── forms/
│   │   │       └── BirthInfoForm.tsx
│   │   ├── lib/
│   │   │   ├── api-client.ts          # BFF API 클라이언트
│   │   │   └── utils.ts
│   │   ├── styles/
│   │   │   └── globals.css
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── next.config.js
│   │
│   └── bff/                    # Koa BFF
│       ├── src/
│       │   ├── index.ts
│       │   ├── app.ts
│       │   ├── routes/
│       │   │   ├── saju.routes.ts
│       │   │   ├── fortune.routes.ts
│       │   │   ├── compatibility.routes.ts
│       │   │   └── user.routes.ts
│       │   ├── controllers/
│       │   │   ├── saju.controller.ts
│       │   │   ├── fortune.controller.ts
│       │   │   └── compatibility.controller.ts
│       │   ├── services/
│       │   │   ├── saju.service.ts
│       │   │   ├── fortune.service.ts
│       │   │   └── compatibility.service.ts
│       │   ├── middleware/
│       │   │   ├── error-handler.ts
│       │   │   ├── logger.ts
│       │   │   └── validator.ts
│       │   ├── utils/
│       │   │   └── response.ts
│       │   └── types/
│       │       └── index.ts
│       ├── package.json
│       └── tsconfig.json
│
├── packages/
│   ├── saju-engine/            # 사주 계산 엔진
│   │   ├── src/
│   │   │   ├── calculator/
│   │   │   │   ├── saju-calculator.ts     # 사주 계산
│   │   │   │   ├── calendar-converter.ts  # 양력/음력 변환
│   │   │   │   └── time-adjuster.ts       # 시간 보정
│   │   │   ├── analyzer/
│   │   │   │   ├── element-analyzer.ts    # 오행 분석
│   │   │   │   ├── luck-analyzer.ts       # 운세 분석
│   │   │   │   └── compatibility.ts       # 궁합 분석
│   │   │   ├── data/
│   │   │   │   ├── stems.ts              # 천간 데이터
│   │   │   │   ├── branches.ts           # 지지 데이터
│   │   │   │   └── interpretations.ts    # 해석 데이터
│   │   │   └── index.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   ├── shared-types/           # 공유 타입 정의
│   │   ├── src/
│   │   │   ├── saju.types.ts
│   │   │   ├── fortune.types.ts
│   │   │   └── user.types.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   │
│   └── ui-components/          # 공유 UI 컴포넌트
│       ├── src/
│       │   ├── Button.tsx
│       │   ├── Card.tsx
│       │   └── index.ts
│       ├── package.json
│       └── tsconfig.json
│
├── deployment/                # 배포 설정
│   ├── docker-compose.yml
│   ├── docker-compose.monitoring.yml
│   ├── nginx/
│   │   └── conf.d/
│   │       └── saju.conf
│   ├── scripts/
│   │   ├── backup.sh
│   │   ├── restore.sh
│   │   └── certbot-renew.sh
│   └── prometheus/
│       └── prometheus.yml
│
├── .github/
│   └── workflows/
│       ├── ci.yml
│       └── deploy.yml
│
├── package.json               # 모노레포 루트
├── pnpm-workspace.yaml
├── turbo.json
├── README.md
└── docs/
    ├── setup-proxmox.md      # Proxmox 설정 가이드
    ├── deployment.md         # 배포 가이드
    └── monitoring.md         # 모니터링 가이드
```

## 8. 개발 우선순위

### Phase 1: MVP (4주)
**목표**: 기본적인 사주 계산 및 조회 기능

**Tasks**:
1. 인프라 및 프로젝트 초기 설정
   - Proxmox VM/LXC 생성 및 기본 설정
   - Docker, Docker Compose 설치
   - Monorepo 구조 설정 (pnpm + turborepo)
   - Next.js 프로젝트 설정 (Type-stripping)
   - Koa BFF 프로젝트 설정
   - Docker Compose 기본 설정

2. 사주 계산 엔진 구현
   - 양력/음력 변환
   - 사주팔자 계산
   - 오행 분석

3. 기본 UI 구현
   - 홈페이지
   - 사주 입력 폼
   - 사주 결과 페이지

4. BFF API 구현
   - 사주 계산 API
   - 기본 에러 핸들링

5. 개발 환경 배포
   - Docker 이미지 빌드
   - 로컬 배포 테스트

### Phase 2: 확장 기능 (4주)
**목표**: 운세 및 궁합 기능 추가

**Tasks**:
1. 운세 시스템 구현
   - 일일/주간/월간/연간 운세 계산
   - 운세 대시보드 UI

2. 궁합 시스템 구현
   - 궁합 계산 알고리즘
   - 궁합 결과 페이지

3. 사용자 기능
   - 사주 저장/관리
   - 즐겨찾기

4. 데이터베이스 연동
   - PostgreSQL 설정
   - 데이터 모델 구현

### Phase 3: 고도화 (4주)
**목표**: UX 개선 및 추가 기능

**Tasks**:
1. UI/UX 개선
   - 반응형 디자인
   - 다크모드
   - 애니메이션 효과

2. 상세 풀이 강화
   - AI 기반 해석 (선택사항)
   - PDF 다운로드

3. 성능 최적화
   - 이미지 최적화
   - 코드 스플리팅
   - API 캐싱

4. 배포 및 모니터링
   - Proxmox VM/LXC 설정
   - Docker Compose 배포
   - Nginx/Traefik 설정
   - SSL 인증서 설정
   - 에러 모니터링 (Sentry)
   - 시스템 모니터링 (Prometheus + Grafana)

### Phase 4: 운영 및 개선 (진행중)
**목표**: 안정화 및 지속적인 개선

**Tasks**:
1. 버그 수정
2. 사용자 피드백 반영
3. 새로운 기능 추가
4. 성능 모니터링 및 개선

## 9. 기술적 고려사항

### 9.1 Type-stripping 적용
```json
// tsconfig.json
{
  "compilerOptions": {
    "verbatimModuleSyntax": true,
    "isolatedModules": true
  }
}
```

### 9.2 모노레포 구조
- **Tool**: pnpm + turborepo
- **장점**:
  - 코드 공유 용이
  - 일관된 의존성 관리
  - 빠른 빌드 (캐싱)

### 9.3 성능 최적화
- Next.js Image Optimization
- React Server Components 활용
- Nginx 정적 파일 캐싱
- Gzip/Brotli 압축
- HTTP/2 지원

### 9.4 보안
- Input Validation (Zod)
- Rate Limiting
- CORS 설정
- XSS/CSRF 방어
- Firewall 설정 (Proxmox 방화벽 + UFW)
- Fail2ban (무차별 대입 공격 방어)

### 9.5 Proxmox 자체 호스팅 구성

#### 9.5.1 인프라 아키텍처
```
[인터넷]
    ↓
[라우터/방화벽]
    ↓
[Proxmox Host]
    ↓
[VM/LXC Container]
    ├── Nginx/Traefik (리버스 프록시)
    ├── Next.js (Frontend)
    ├── Koa BFF (Backend)
    └── PostgreSQL (Database)
```

#### 9.5.2 컨테이너 구성 옵션

**옵션 1: 단일 VM/LXC에 모든 서비스**
- 장점: 관리 간편, 리소스 효율적
- 단점: 서비스 간 격리 부족
- 권장: 개발/테스트 환경

**옵션 2: 서비스별 분리 (권장)**
- VM/LXC 1: Nginx + Let's Encrypt
- VM/LXC 2: Next.js + Koa BFF
- VM/LXC 3: PostgreSQL
- 장점: 서비스 격리, 확장성
- 단점: 리소스 사용량 증가

#### 9.5.3 Docker Compose 구성
```yaml
# docker-compose.yml
version: '3.8'

services:
  frontend:
    build: ./apps/web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - BFF_URL=http://bff:4000
    depends_on:
      - bff

  bff:
    build: ./apps/bff
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://user:pass@db:5432/saju
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=saju
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=secure_password

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    depends_on:
      - frontend

volumes:
  postgres_data:
```

#### 9.5.4 Nginx 설정 예시
```nginx
# /etc/nginx/conf.d/saju.conf
upstream frontend {
    server frontend:3000;
}

upstream bff {
    server bff:4000;
}

server {
    listen 80;
    server_name yourdomain.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name yourdomain.com;

    ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;

    # Frontend
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # BFF API
    location /api {
        proxy_pass http://bff;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Gzip 압축
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

#### 9.5.5 SSL 인증서 자동 갱신
```bash
# certbot-renew.sh
#!/bin/bash
docker-compose exec nginx nginx -s reload
certbot renew --webroot -w /var/www/certbot
docker-compose exec nginx nginx -s reload
```

```cron
# crontab -e
0 0 * * 0 /path/to/certbot-renew.sh
```

#### 9.5.6 백업 전략
1. **데이터베이스 백업**
   ```bash
   # daily-backup.sh
   #!/bin/bash
   docker-compose exec -T db pg_dump -U user saju > backup_$(date +%Y%m%d).sql
   # 원격 저장소로 전송 (rsync, rclone 등)
   ```

2. **볼륨 백업**
   - Proxmox의 스냅샷 기능 활용
   - 주간 풀 백업
   - 일일 증분 백업

3. **설정 파일 백업**
   - Git 저장소에 버전 관리
   - Docker Compose, Nginx 설정 등

#### 9.5.7 모니터링 설정
```yaml
# docker-compose.monitoring.yml
services:
  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin

  node-exporter:
    image: prom/node-exporter
    ports:
      - "9100:9100"

  uptime-kuma:
    image: louislam/uptime-kuma:1
    ports:
      - "3002:3001"
    volumes:
      - uptime_kuma_data:/app/data
```

#### 9.5.8 네트워크 설정
1. **포트 포워딩** (라우터 설정)
   - 80 → Proxmox VM IP:80
   - 443 → Proxmox VM IP:443

2. **방화벽 규칙**
   ```bash
   # UFW 설정
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw allow 22/tcp  # SSH (필요시)
   ufw enable
   ```

3. **DDNS 설정** (유동 IP 사용시)
   - DuckDNS, No-IP 등 활용
   - 자동 IP 업데이트 스크립트

#### 9.5.9 CI/CD 파이프라인
```yaml
# .github/workflows/deploy.yml
name: Deploy to Proxmox

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted  # Proxmox에서 실행
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker images
        run: docker-compose build

      - name: Deploy
        run: |
          docker-compose down
          docker-compose up -d

      - name: Health check
        run: |
          sleep 10
          curl -f http://localhost:3000 || exit 1
```

#### 9.5.10 리소스 요구사항
**최소 사양** (단일 VM/LXC):
- CPU: 2 cores
- RAM: 4GB
- Storage: 20GB SSD

**권장 사양** (프로덕션):
- CPU: 4 cores
- RAM: 8GB
- Storage: 50GB SSD
- 네트워크: 100Mbps 이상

## 10. 비기능적 요구사항

### 10.1 성능
- 초기 로딩 시간: < 3초
- API 응답 시간: < 500ms
- Lighthouse Score: > 90점

### 10.2 접근성
- WCAG 2.1 AA 레벨 준수
- 키보드 네비게이션 지원
- 스크린 리더 지원

### 10.3 브라우저 지원
- Chrome (최신 2개 버전)
- Safari (최신 2개 버전)
- Firefox (최신 2개 버전)
- Edge (최신 2개 버전)
- 모바일 브라우저 (iOS Safari, Chrome Mobile)

### 10.4 반응형 디자인
- Desktop: 1024px 이상
- Tablet: 768px ~ 1023px
- Mobile: ~ 767px

## 11. 향후 확장 가능성

### 11.1 추가 기능
- 프리미엄 상세 풀이 (유료)
- 전문가 상담 예약
- 커뮤니티 기능
- 일정 관리 (길일 알림)
- 소셜 공유 기능

### 11.2 다국어 지원
- 한국어 (기본)
- 영어
- 중국어
- 일본어

### 11.3 모바일 앱
- React Native 기반
- 동일한 BFF 사용

## 12. 리스크 및 대응 방안

### 12.1 기술적 리스크
**리스크**: 사주 계산 알고리즘의 정확성
**대응**: 기존 검증된 라이브러리 참고 및 전문가 검수

**리스크**: Type-stripping 관련 호환성 이슈
**대응**: 최신 Node.js 버전 사용 및 충분한 테스트

### 12.2 비즈니스 리스크
**리스크**: 낮은 사용자 유입
**대응**: SEO 최적화, SNS 마케팅

**리스크**: 콘텐츠의 정확성 논란
**대응**: 명확한 면책 조항, 전문가 감수

## 13. 성공 지표 (KPI)

### 13.1 사용자 지표
- 월간 활성 사용자(MAU): 목표 10,000명
- 일일 활성 사용자(DAU): 목표 1,000명
- 재방문율: 목표 40%

### 13.2 기술 지표
- 서버 가동률: 99.9%
- API 평균 응답 시간: < 500ms
- 에러율: < 0.1%

### 13.3 비즈니스 지표
- 사주 계산 수: 월 10,000건
- 페이지뷰: 월 50,000PV
- 평균 세션 시간: 5분 이상

---

## 부록

### A. 참고 자료
- 사주명리학 기초 이론서
- 만세력 데이터
- 유사 서비스 벤치마킹

### B. 용어 정리
- **천간(天干)**: 갑을병정무기경신임계 (10가지)
- **지지(地支)**: 자축인묘진사오미신유술해 (12가지)
- **오행(五行)**: 목화토금수
- **십성(十星)**: 비겁, 식상, 재성, 관성, 인성
- **십이운성**: 장생, 목욕, 관대, 건록, 제왕 등

### C. 문의
- 프로젝트 관련 문의: [이메일/연락처]
- 기술 문의: [개발팀 이메일]

---

**문서 버전**: 1.0
**최종 수정일**: 2025-11-12
**작성자**: Claude (AI Assistant)
